-- Certifique-se de ter a biblioteca "Orion Library" carregada
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/shlexware/Orion/main/source"))()

-- Criação da GUI
local Window = Library:MakeWindow({Name = "Meme Sea Auto Farm", HidePremium = false, SaveConfig = true, ConfigFolder = "MemeSeaConfig"})

-- Auto Farm
local AutoFarmTab = Window:MakeTab({Name = "Auto Farm", Icon = "rbxassetid://4483345998", PremiumOnly = false})
AutoFarmTab:AddToggle({
    Name = "Auto Farm",
    Default = false,
    Callback = function(Value)
        -- Código para a funcionalidade de Auto Farm
    end    
})

-- Auto Boss
local AutoBossTab = Window:MakeTab({Name = "Auto Boss", Icon = "rbxassetid://4483345998", PremiumOnly = false})
AutoBossTab:AddToggle({
    Name = "Auto Boss",
    Default = false,
    Callback = function(Value)
        -- Código para funcionalidade de Auto Boss
    end    
})

-- Auto Stats
local AutoStatsTab = Window:MakeTab({Name = "Auto Stats", Icon = "rbxassetid://4483345998", PremiumOnly = false})
AutoStatsTab:AddToggle({
    Name = "Auto Stats",
    Default = false,
    Callback = function(Value)
        -- Código para funcionalidade de Auto Stats
    end    
})

-- Auto Detect Level
local AutoDetectLevelTab = Window:MakeTab({Name = "Auto Detect Level", Icon = "rbxassetid://4483345998", PremiumOnly = false})
AutoDetectLevelTab:AddToggle({
    Name = "Auto Detect Level",
    Default = false,
    Callback = function(Value)
        -- Código para funcionalidade de Auto Detect Level
    end    
})

-- Auto Raid
local AutoRaidTab = Window:MakeTab({Name = "Auto Raid", Icon = "rbxassetid://4483345998", PremiumOnly = false})
AutoRaidTab:AddToggle({
    Name = "Auto Raid",
    Default = false,
    Callback = function(Value)
        -- Código para funcionalidade de Auto Raid
    end    
})

Library:Init()
